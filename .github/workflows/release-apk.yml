# name: Build and Release APK (Capacitor) — replaced by release-tauri.yml
#
# on:
#   push:
#     tags:
#       - 'v*'
#   workflow_dispatch:
#
# permissions:
#   contents: write
#
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#
#       - uses: pnpm/action-setup@v4
#         with:
#           version: 9
#
#       - uses: actions/setup-node@v4
#         with:
#           node-version: 22
#           cache: pnpm
#
#       - name: Set up JDK 17
#         uses: actions/setup-java@v4
#         with:
#           distribution: temurin
#           java-version: 17
#
#       - name: Accept Android SDK licenses
#         run: |
#           yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1 || true
#
#       - name: Install dependencies
#         run: pnpm install --frozen-lockfile
#
#       - name: Build web app
#         run: pnpm build
#
#       - name: Sync Capacitor Android
#         run: npx cap sync android
#
#       - name: Build debug APK
#         run: |
#           cd android
#           chmod +x gradlew
#           ./gradlew assembleDebug
#
#       - name: Rename APK with version
#         run: |
#           VERSION="${GITHUB_REF_NAME#v}"
#           cp android/app/build/outputs/apk/debug/app-debug.apk "android/app/build/outputs/apk/debug/daily-habit-${VERSION}.apk"
#
#       - name: Upload APK as artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: daily-habit-apk
#           path: android/app/build/outputs/apk/debug/daily-habit-*.apk
#
#       - name: Create Release
#         if: startsWith(github.ref, 'refs/tags/')
#         uses: softprops/action-gh-release@v2
#         with:
#           files: android/app/build/outputs/apk/debug/daily-habit-*.apk
#           name: Daily Habit ${{ github.ref_name }}
#           draft: false
#           prerelease: contains(github.ref_name, '-')
#           generate_release_notes: true
